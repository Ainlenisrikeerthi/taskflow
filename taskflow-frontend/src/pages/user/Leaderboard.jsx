import {useEffect,useState} from "react";import {api} from "../../data/api";import {Trophy} from "lucide-react";
export default function Leaderboard(){const [rows,setRows]=useState([]);useEffect(()=>{api.coding.leaderboard(20).then(setRows).catch(()=>setRows([]))},[]);return <div><div className="page-header"><div className="page-header-left"><h1>DSA Leaderboard</h1><p>Top performers ranked by best score per coding task.</p></div></div><div className="card leaderboard-card"><div className="leaderboard-head"><Trophy size={20}/>Top 20</div><div className="table-scroll"><table className="premium-table"><thead><tr><th>Rank</th><th>User</th><th>Problems</th><th>Total Score</th><th>Average /5</th></tr></thead><tbody>{rows.map(r=><tr key={r.userId}><td><strong>#{r.rank}</strong></td><td>{r.userName}</td><td>{r.solved}</td><td>{r.totalScore}</td><td>{r.averageScore}</td></tr>)}</tbody></table></div>{!rows.length&&<div style={{padding:30,textAlign:"center"}}>No coding submissions yet.</div>}</div></div>}
